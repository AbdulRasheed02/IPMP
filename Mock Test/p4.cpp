#include <iostream>
using namespace std;
int main()
{
    int queries;
    cin >> queries;
    for (int i = 0; i < queries; i++)
    {
        int n, m, k;
        cin >> n >> m >> k;
        int array[n];
        for (int i = 0; i < n; i++)
        {
            cin >> array[i];
        }
        }
}